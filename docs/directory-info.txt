This file provides documentation (for the benefit of doxygen) for directories
with documented files. The use of c-style comments is rather a hack; I can't
tell if there's a better way to make doxygen parse commands correctly.

/**
 * @dir src
 * Source files for Q
 */

/**
 * @dir include
 * Header files for Q
 */
